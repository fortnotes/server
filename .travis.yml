language: node_js
node_js:
    - "0.12"
    - "0.10"
sudo: false
services:
    - mysql
before_script:
    - mysql -e 'create database fortnotes;'
    - node ./bin/cli.js --config ../config/mysql.json
install:
    - npm install
cache:
    directories:
        - node_modules
